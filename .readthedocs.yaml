version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.12"
  commands:
    - pip install uv
    - uv sync --locked --group docs
    - uv run python -m sphinx -W --keep-going -b html docs "$READTHEDOCS_OUTPUT/html"
